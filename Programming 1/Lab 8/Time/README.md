Write the following function assuming that you have a time structure that contains three

members: hours, minutes and seconds (all of type int).

struct time split time(long long total seconds);

total seconds is a time represented as the number of seconds since midnight. The function

should return a structure containing the equivalent time in hours (0-23), minutes (0-59), and

seconds (0-59).
